// Definindo variáveis
const nome = 'João';
const idade = 25;
const profissao = 'engenheiro';

// Usando template literals para criar uma string
const mensagem = `Olá, meu nome é ${nome}, tenho ${idade} anos e sou ${profissao}.`;

// Imprimindo a mensagem no console
console.log(mensagem);
